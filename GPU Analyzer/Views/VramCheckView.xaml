<UserControl x:Class="GPU_Analyzer.Views.VramCheckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GPU_Analyzer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Start VRAM Check" Command="{Binding StartVramTestCommand}" Width="160" Height="34"/>
        <TextBox Grid.Row="1" Text="{Binding VramResult}" Margin="0,8,0,0" AcceptsReturn="True" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</UserControl>
